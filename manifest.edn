{:actor/id        "tedai"
 :actor/glyph     "代"
 :actor/tier      :tier-b
 :actor/integrates ["karakuri" "tazuna"]
 :actor/domain    "tedai.etzhayyim.com"
 :actor/did       "did:web:etzhayyim.com:actor:tedai"
 :actor/purpose   "Member-computer operation (手代 = the Edo merchant-house clerk who operates the house's affairs on the master's instruction and under the master's seal — member-principal by etymology). Gives a member a uniform command vocabulary (DesktopOp) over their OWN computer — desktop apps, files, OS surfaces — driving the safest adapter (T1 scripting/accessibility-API > T2 stance-permitted vision-pointer w/ ON-DEVICE vision > T3 file-level), logging every op to the kotoba Datom log. karakuri's OS-layer sibling (web-service : karakuri = computer : tedai = robot body : tazuna). The charter-clean inverse of commercial computer-use: own-device-only, T1-first, stance-honest (no anti-cheat/DRM/detection evasion), no-server-key, member-signed mutate, on-device vision (a screenshot never leaves the device), no-surveillance (never bossware), hash-only evidence."
 :actor/status    :r0
 :actor/adr       "2606101400"
 :actor/related   ["2606039200" "2605231525" "2605215000" "2605241900" "2605181100" "2605192200" "2606034800" "2606042100" "2606033600" "2605302130" "2606013800"]
 :actor/labor
 {:isic   "J6201"
  :isco   ["2512" "3514"]
  :unspsc ["81112"]
  :lps-rank nil}

 :actor/charter-rider {:rider/version "3.0" :rider/applied true}

 :actor/adapter-tiers
 [{:tier/id "T1" :tier/kind :scripting-api :tier/preference 1 :tier/note "preferred — the OS's official automation surface (AppleScript/JXA + AXUIElement, Windows UI Automation, AT-SPI2/D-Bus, or the app's own CLI); deterministic, semantic, permission-dialog mediated"}
  {:tier/id "T2" :tier/kind :vision-pointer :tier/engine :on-device-vision :tier/preference 2 :tier/note "computer-use shape (screenshot → locate → click/type) on the member's OWN paired device, ONLY where the app's synthetic-input stance permits (G2); vision inference ON-DEVICE (baien edge, ADR-2605241900) or LAN Murakumo — a frame never leaves the device (G4); surveillance + evasion verbs structurally unrepresentable (methods/t2_vision.py). Anti-cheat games / DRM players / banking apps are :prohibited → T2 refused, T3 only"}
  {:tier/id "T3" :tier/kind :file-level :tier/preference 3 :tier/note "operate on the app's documents/files directly; the data-portability leg (many GUI tasks are really file transforms)"}]

 :actor/gates
 [{:gate/id "G1" :gate/name "member-principal-own-device-only" :gate/req "operates ONLY a device the member owns + has physically paired (consent ceremony, member-held pairing key); no third-party device control, no remote-admin-of-others; structurally not a RAT"}
  {:gate/id "G2" :gate/name "t1-preferred-stance-honest" :gate/req "prefer the OS's official automation surface; T2 vision-pointer ONLY where the app's synthetic-input stance permits; NO detection-evasion (no anti-cheat bypass, no DRM circumvention, no input-spoofing-to-defeat-bot-detection, no driver-level input forgery); :prohibited stance refuses T2 by construction; missing stance = default-deny"}
  {:gate/id "G3" :gate/name "no-server-key" :gate/req "pairing keys + device sessions member-held, encrypted (com.etzhayyim.encrypted.*, ADR-2605181100), never platform-held; mutating actuation authorized by member signature; server signature refused (ADR-2605231525)"}
  {:gate/id "G4" :gate/name "murakumo-only-on-device-vision" :gate/req "NL->plan via LiteLLM 127.0.0.1:4000 only (ADR-2605215000); T2 vision inference on-device (baien edge, ADR-2605241900) or LAN Murakumo; a screenshot NEVER leaves the device — cloud computer-use APIs structurally unrepresentable"}
  {:gate/id "G5" :gate/name "read-default-mutate-gated-outward-held" :gate/req ":read ships at R0 (plan only); :create/:update/destructive :delete require member-sig + dry-run confirm; :outward ops (send/pay/post/upload — effect leaves the device) additionally require the Council outward gate; a member signature is necessary but NOT sufficient for :outward"}
  {:gate/id "G6" :gate/name "actuation-gated" :gate/req "ANY live input injection (click/keystroke/file mutation) Council Lv6+ + operator gated; R0 = offline parse/plan/dry-run only; the single live membrane is methods/actuate_live.py (refuses unless env flag + operator + Council + member-sig; NotImplementedError at R0 even then)"}
  {:gate/id "G7" :gate/name "kotoba-eavt-audit" :gate/req "every planned + executed DesktopOp written to the kotoba Datom log (as-of, replayable); member can audit exactly what touched their machine"}
  {:gate/id "G8" :gate/name "no-surveillance" :gate/req "screen observation consent-scoped to the member's OWN session for the DURATION of an op; no ambient watching, no idle/presence monitoring, no keylogging, no camera/microphone capture, no observation of other persons; NEVER bossware / parental-stalkerware / partner-surveillance; surveillance verbs structurally unrepresentable (t2_vision.py); extends manako on-device no-biometric precedent (ADR-2606034800)"}
  {:gate/id "G9" :gate/name "evidence-minimization" :gate/req "audit trail stores sha256 hashes + structured summaries of evidence, never raw frames; raw screenshots stay on-device under the member's key and expire; flag KEYS only (never values) serialized into the Datom log"}]

 :actor/non-goals
 [{:ng/id "N1" :ng/req "not bossware / employee-monitoring / parental-stalkerware / partner-surveillance"}
  {:ng/id "N2" :ng/req "no anti-cheat / DRM / bot-detection evasion or driver-level input forgery"}
  {:ng/id "N3" :ng/req "not a RAT / botnet — no control of any unpaired or third-party device"}
  {:ng/id "N4" :ng/req "no keylogging or credential harvesting (of anyone, incl. the member — credentials are typed by the member, never by tedai)"}
  {:ng/id "N5" :ng/req "not a click-farm / ad-fraud / fake-engagement / mass-automation engine"}
  {:ng/id "N6" :ng/req "no driving of prohibited-content systems (Charter-Rider §2)"}
  {:ng/id "N7" :ng/req "not a browser-automation tool — browser surfaces route to karakuri (one owner per surface)"}]

 :actor/cells
 [{:cell/id "app_resolve"     :cell/kind :langgraph :cell/runtime :wasm :cell/node "dan"}
  {:cell/id "intent_plan"     :cell/kind :langgraph :cell/runtime :wasm :cell/node "naphtali"}
  {:cell/id "pairing_broker"  :cell/kind :langgraph :cell/runtime :wasm :cell/node "gad"}
  {:cell/id "actuate_invoke"  :cell/kind :langgraph :cell/runtime :wasm :cell/node "asher"}
  {:cell/id "evidence_audit"  :cell/kind :langgraph :cell/runtime :wasm :cell/node "joseph"}]

 :actor/lex
 [{:lex/id "appCapabilityRecord"}
  {:lex/id "desktopOp"}
  {:lex/id "intentPlan"}
  {:lex/id "pairingGrant"}
  {:lex/id "evidenceRecord"}]}
